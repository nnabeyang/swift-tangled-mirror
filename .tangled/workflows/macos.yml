when:
  - event: ["manual"]

engine: "agent"
platform: "macos"
executor: "virtual-machine"
xcode: "26.6"

clone:
  submodules: false

steps:
  - name: "Test"
    command: |
      set -euo pipefail
      env -u TNG_AUTH_AGENT swift test --disable-default-traits
