when:
  - event: ["manual"]

engine: "agent"
platform: "macos"
executor: "virtual-machine"
xcode: "26.6"

clone:
  submodules: false

steps:
  - name: "Documentation"
    command: |
      set -euo pipefail
      ./scripts/generate-documentation.sh --check
